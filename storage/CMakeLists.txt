# CMake Build Configuration: PKM Storage Layer Module

This configuration manages the compilation of storage security components for the PKM framework, specifically handling Universal Flash Storage (UFS) and embedded MultiMediaCard (eMMC) secure purge operations, alongside flash memory write-protection mechanisms.