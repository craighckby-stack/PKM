# CMake Build Configuration: PKM TrustZone TEE Duress Handler and Secure World Components

This document defines the CMake build configuration for the PKM TrustZone Trusted Execution Environment (TEE) Duress Handler and its associated secure world components.